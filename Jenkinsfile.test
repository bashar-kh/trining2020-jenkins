#!/bin/groovy
pipeline {
    agent {
        docker {
            image 'fedora'
        }
    }

    stages {
        stage('Init') {
            steps {
                copyArtifacts(projectName: 'bkh_build_rpm')
                sh('ls')
            }
        }
    }
}
