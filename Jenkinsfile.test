#!/bin/groovy
pipeline {
    agent {
        docker {
            image 'fedora'
	     args '-u 0'
        }
    }

    stages {
        stage('Init') {
            steps {
                copyArtifacts(projectName: 'bkh_build_rpm')
                sh('ls')
            }
        }
        stage('test') {
            steps {
                sh('dnf install -y hello-[^d]*rpm')
                sh('hello')
            }
        }
    }
}
